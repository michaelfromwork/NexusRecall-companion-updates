# NexusRecall Companion 0.1.0 (96)

Private-beta tip (official Node; Developer ID + notarized).

- Clipped-full continuation (`f6-clippedFullContinuation-v1`): prefix-kept `scope:"full"` that hits the char budget advertises one tail `scope:"range"` continuation instead of a neighborhood fan-out
- Weak-match full-scope recovery (`f6-weakMatchFullScope-v1`): empty primary + body-available weak lead → `get_record_context` `scope:"full"`
- Includes tip 94 platform filter vocabulary plus sanitized external MCP (P2.44-Ext bake on `DISTRIBUTION=1`)
