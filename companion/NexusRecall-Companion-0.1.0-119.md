# NexusRecall Companion 0.1.0 (119)

Private-beta tip (official Node; Developer ID + notarized). **Briefs ship** — v2 Day 0 Companion.

- Enable Briefs once after Linked + Connected (Codex login required). Claude Desktop prefers official `.mcpb` (`nxr-briefs`); ChatGPT Desktop uses the existing Connect tunnel (recycle Connect after update)
- Create brief on any non-empty folder (name = folder). Named `get_brief` / `update_brief`. First-compile vs maintain Codex prompts
- Schema `g-brief-ship-v1`. Soft tip (no `minimumRequiredBuild`). Includes tip 96 retrieval + sanitized external MCP (P2.44-Ext bake on `DISTRIBUTION=1`)
